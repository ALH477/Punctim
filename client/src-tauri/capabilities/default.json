{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Punctim comms client main window (events + window). All comms/audio/recording go through custom #[tauri::command]s, which need no extra permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
